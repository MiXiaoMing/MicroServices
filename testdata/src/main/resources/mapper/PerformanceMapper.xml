<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.microservices.testdata.mapper.PerformanceMapper">

    <sql id="columns">
		  id, schemeID as scheme_id, cpuRatio as cpu_ratio,
		  memorySize as memory_size, flag
    </sql>

    <insert id="insertPerformance" parameterType="com.microservices.testdata.entity.Performance">
        insert into td_performance
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="schemeID != null">
                scheme_id,
            </if>
            <if test="cpuRatio != null">
                cpu_ratio,
            </if>
            <if test="memorySize != null">
                memory_size,
            </if>
            <if test="flag != null">
                flag,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=VARCHAR},
            </if>
            <if test="schemeID != null">
                #{schemeID,jdbcType=VARCHAR},
            </if>
            <if test="cpuRatio != null">
                #{cpuRatio,jdbcType=VARCHAR},
            </if>
            <if test="memorySize != null">
                #{memorySize,jdbcType=VARCHAR},
            </if>
            <if test="flag != null">
                #{flag,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <select id="selectPerformancesBySchemeID" resultType="com.microservices.testdata.entity.Performance" parameterType="java.util.Map">
        select
        <include refid="columns"/>
        <![CDATA[
            from td_performance
            where scheme_id = #{schemeID,jdbcType=VARCHAR}
            and flag <> 'D'
        ]]>
    </select>

</mapper>